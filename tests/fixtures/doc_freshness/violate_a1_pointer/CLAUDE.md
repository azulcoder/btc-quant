# Rules
The promotion bar: in `STRATEGY.md`, grep `` `DSR>0.95` net-of-cost ``.
Migration state changes hourly and lives in `docs/STATUS.md` — pointer, not a claim.
The promotion bar is at `STRATEGY.md:804` — a line number that will rot.
